# 5. Default Arguments
def increment(number, by=1):
    return number + by
print(increment(2, 5 ))

# making an optional parameter
# And it should come after the default value( the required parameter  not before it

# Happy coding